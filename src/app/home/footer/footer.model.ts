/* Define model for footer in order to populate the footer section correctly. */
export class Footer {
    public heading: string; // A footer should have a heading property of type string.

    constructor(heading: string) {
        this.heading = heading;
    }
}
